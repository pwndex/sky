PyQt6>=6.6

# macOS only — Accessibility trust check, CGEventTap, and floating above fullscreen.
# Harmless to install elsewhere but Mac-specific.
pyobjc-framework-ApplicationServices>=9.0 ; sys_platform == "darwin"
pyobjc-framework-Cocoa>=9.0 ; sys_platform == "darwin"
